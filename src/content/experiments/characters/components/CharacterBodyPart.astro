---
export interface Props {
  name: string;
}

const { name } = Astro.props;
const parts = ["front", "left", "right", "back", "top", "bottom"];
---

<div class:list={["part", name]}>
  {parts.map((part) => <div class:list={["part-side", `part-side-${part}`]} />)}
</div>
