---
import type { Icons } from "@/types/common";

export interface Props {
  icon: Icons;
  className?: string;
}

const { className, icon } = Astro.props;
---

<svg class:list={["icon", "lucide-icon", className]} aria-hidden="true">
  <use href={`/icons.svg#${icon}`}></use>
</svg>
